<h1><%= @event.name %> </h1>
<p> <%= link_to("Home", events_path) %> | <%= link_to("Edit Event", edit_event_path) %> | <%= link_to 'Delete', event_path(@event), method: :delete, data: {confirm: "Are you sure you want to delete the event '#{@event.name}'? This cannot be undone!"}%> </p>

<%= render 'single_event', event: @event %>

<p>This is going to be a real good time!</p>
